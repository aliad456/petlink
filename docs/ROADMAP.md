# Kami — מפת דרכים ומצב נוכחי

> מסמך חי. מעדכנים אותו בסוף כל שלב. האפיון המלא ב-`docs/SPEC.md`.

## מה כבר בנוי ✅

| שלב | מצב |
|---|---|
| **1. תשתית** | הושלם: מסד נתונים, התחברות, הרשאות, 2FA למנהלים, יומן פעולות |
| **עיצוב** | הושלם: Liquid Glass, רקע ציורי חיות (סגנון וואטסאפ), header, לוגו Kami עם אנימציה, favicon ואייקון אפליקציה, מעברי עמודים, מצב כהה |
| **2. פאנל ניהול** | משתמשים ✅ · קטגוריות ופילטרים ✅ · עסקים ✅ · יומן פעולות ⏳ · מנהלים והרשאות ⏳ (נדחה לבקשת הבעלים) |
| **4. חיפוש** | הושלם: חיפוש חופשי עם הבנת עיר וקטגוריה ("וטרינר ברמת גן"), השלמה אוטומטית, עמודי קטגוריה (`/vets`), פילטרים (פתוח עכשיו, קרוב אליי, עיר, כן/לא, בחירה מרובה), מרחק ממרכז העיר, "חדשים ב-Kami" בדף הבית |
| **ביצועים** | הושלם: שרתים בפרנקפורט ליד מסד הנתונים, header קבוע ב-layout, שלדי טעינה בכל עמוד, קאש לנתוני יסוד, טעינה מראש של קטגוריות, פס קטגוריות בעמודי החיפוש, תמונות מוקטנות (AVIF), אפקטים קלים לגלילה בטלפון, עמוד 404 בעברית |
| **עמודים לא מנוהלים** | הושלם: הצוות מוסיף עסקים קיימים ממידע ציבורי (פאנל → עסקים → "הוספת עסק"), העמוד מסומן "לא מנוהל" (בלי וי כחול, בלי ניחוש וואטסאפ). בעל העסק לוחץ "זה העסק שלך?" ושולח בקשת בעלות או הסרה; הצוות מאמת בטלפון המפורסם של העסק ומאשר ב"בקשות בעלות". הכול ביומן הפעולות |
| **בטא ומשפטי** | הושלם: תגית BETA ליד הלוגו, פס הודעה (נסגר ונזכר בעוגייה), פוטר. תנאי שימוש, מדיניות פרטיות, תנאים לבעלי עסקים, הצהרת נגישות, צור קשר + תיבת "פניות" בפאנל. תיעוד הסכמה בהרשמה (גרסת תנאים) והסכמה נפרדת לדיוור, עם כיבוי בעמוד החשבון |
| **5. ביקורות** | הושלם: דירוג 1–5 + טקסט, עולה מיד (שם פרטי + אות משפחה), ביקורת אחת למשתמש לעסק (עריכה/מחיקה), תגובה פומבית של בעל העסק, דיווח עם סיבה, הסתרה אוטומטית אחרי 3 דיווחים, תור טיפול בפאנל (להשאיר / להסיר עם סיבה, ביומן). ממוצע ומספר ביקורות בעמוד ובכרטיסים. אין ביקורות על עמודים לא מנוהלים ועל העסק של עצמך |
| **מודעות וימי אימוץ** | הושלם (מתוך שלב 8): 4 מיקומים — דף הבית, עמודי קטגוריה, בין תוצאות החיפוש (עד 3 מתחלפות ביום, כמות ניתנת לשינוי) ופופאפ בכניסה (אחד ליום, פעם ביום לגולש, סגירה אחרי 3 שניות). מידות לכל מיקום נקבעות בפאנל (ברירת מחדל: פס 1200×300 בתחתית דף הבית, פופאפ 1080×1920), ונבדקות בהעלאה. תצוגה מקדימה חיה בטלפון ובמחשב בזמן יצירת המודעה, וקישור לתצוגה מקדימה לשליחה למפרסם. לוח שנה לכל מיקום, בחירת ימים לא רצופים, ימים שמורים (חוק קבוע לפי יום בשבוע + תאריכים מיוחדים) עם אזהרה ואפשרות לעקוף. חשיפות ולחיצות לכל מודעה + דוח להדפסה. הסכום ששולם גלוי רק לבעלים. תווית "ממומן" / "יום אימוץ" |
| **חיית המחמד שלי** | הושלם: פרופיל לכל חיה (עד 10 לחשבון בעל חיה): תמונת פרופיל + 12 תמונות, סוג, גזע, מין, גיל, משקל, עיקור, שבב, הערות בריאות ואופי, חיסונים עם "הבא בתור". אימוץ: מעמותה ב-Kami (מונה "חיות שאומצו מכאן" בעמוד העמותה) או טקסט חופשי. שיתוף: עם עסק ב-Kami (כפתור בעמוד העסק; העסק רואה ב"חיות ששותפו איתי" עם שם וטלפון) או בקישור סודי (`/pet/<token>`). פרטי כברירת מחדל |
| **סטטיסטיקות** | הושלם: בדף הראשי של הפאנל (הרשאת "צפייה בדשבורד"): "עכשיו באתר" (מחוברים/אורחים, מתעדכן כל 15 שניות), מבקרים, צפיות, מחוברים, אורחים, הרשמות ועסקים חדשים להיום / 3 ימים / שבוע / חודש, גרף 30 יום, דפים ועסקים נצפים, מאיפה הגיעו. בלי עוגיות ובלי גוגל אנליטיקס: מזהה מבקר מוצפן שמתחלף כל יום, IP לא נשמר, בוטים לא נספרים, נמחק אחרי שנה |
| **הוספת עסקים** | הושלם: בדיאלוג "הוספת עסק" גם שעות פעילות, חגים ומאפיינים (פילטרים של התחום). "ייבוא מטבלה": הדבקה מאקסל/גוגל שיטס או קובץ CSV, תבנית להורדה, בדיקה של כל שורה לפני שמירה, הכול או כלום |
| **גוגל ושיתוף** | הושלם: תמונת שיתוף לאתר (וואטסאפ / X / פייסבוק), `sitemap.xml`, `robots.txt`, נתוני LocalBusiness לגוגל בכל עמוד עסק. נשאר: לחבר את Search Console אחרי הדומיין |
| **3. עמוד עסק** | הושלם: עמוד בסגנון פרופיל (רקע, פרופיל, בועת סטטוס, פס הישגים, פרטים, שעות, גלריה, מחירון), רישום, עורך עם תצוגה חיה, סינון מילים גסות במסד, אישור בפאנל. PRO — חלון "בקרוב" + רשימת המתנה |

## מה נשאר

| שלב | מה כולל |
|---|---|
| **2 (המשך)** | צפייה ביומן הפעולות, הוספת עובדים |
| **6. מנויים וסליקה** | PetLink PRO ב-39.90 ₪, מסלול סטודנטים עם קוד, ספק סליקה ישראלי |
| **7. עורך AI** | עיצוב עמוד העסק, כתיבת "אודות" ומחירון בעזרת AI |
| **מנה 1 (מעורבות)** | הושלם: כפתור חירום בדף הבית (קטגוריה שמסומנת בפאנל ← פתוחים עכשיו + הכי קרובים), מבצע לכל עסק עם תאריך סיום (עד 60 יום) + "מבצעים השבוע" בדף הבית ועמוד `/deals`, מועדפים (לב בעמוד ובכרטיסים, "העסקים ששמרתי" בחשבון), תמונת שיתוף לכל עסק (וואטסאפ/פייסבוק) |
| **מנה 2** | הושלם: תפריט נגישות (טקסט, ניגודיות, עצירת אנימציות, קישורים, גופן קריא — נשמר בדפדפן), "הוספה למסך הבית" (כפתור באנדרואיד/מחשב, הוראות באייפון, מהביקור השני), ימי אימוץ = ערימת פוסטרים של עמותות בעמוד הקטגוריה המסומנת (מיקום מודעות "עמוד ימי אימוץ", בלי הגבלה, כל גודל). גלריית חיות לאימוץ — נדחתה |
| **8. השלמות** | הכנסות בדשבורד |

**סדר מומלץ:** למלא את המדריך בעסקים (פניות יזומות + "הוספת עסק") ← שלב 6 (סליקה + PRO). אחרי אלה אפשר להתחיל למלא את המדריך.


## לפני ההשקה
- שירות מייל (SMTP, למשל Resend), כדי שמיילי אישור ואיפוס יגיעו לכל משתמש. אחר כך לעדכן את תבניות המייל לקישורי `token_hash` (ראו `src/app/auth/callback/route.ts`).
- **פרטי המפעיל** ב-`src/lib/legal.ts`: השם "א.ב פתרונות" (עוסק מורשה) כבר מוזן. חסרים מיילים ורכז/ת נגישות (מוצגים בסוגריים מרובעים). מספר העוסק = ת.ז., ולכן לא מפורסם; לפני מכירה לצרכנים צריך להציג אותו בשלב התשלום (חוק הגנת הצרכן, ס' 14ג) או לפתוח חברה בע"מ.
- **בדיקת עורך דין** לתנאים ולמדיניות (הנוסח נכתב לפי החוק, אבל אינו תחליף לייעוץ משפטי).
- בדיקת נגישות ידנית (קורא מסך, מקלדת) ועדכון תאריך ההצהרה.
- לכבד את התחייבות המחיקה במדיניות: בקשת מחיקה → מחיקה תוך 30 יום (כרגע ידנית, מהפאנל).
- בחירת דומיין ל-Kami וחיבורו (ואז לעדכן `NEXT_PUBLIC_SITE_URL` — ממנו נבנים ה-sitemap ותמונות השיתוף), ורישום ב-Google Search Console עם ה-sitemap.

## החלטות

| נושא | החלטה |
|---|---|
| שם ומיתוג | **Kami** (לשעבר PetLink). לוגו: K עם טבעת זוהרת, שנפתח ל-"Kami" במעבר עכבר. הדומיין עדיין לא נקבע |
| פלטפורמה | אתר מובייל-קודם + PWA (נימוקים ב-`docs/STACK.md`) |
| אזור השקה | **כל הארץ.** ממלאים בהדרגה בפניות יזומות לעסקים |
| עיצוב | Liquid Glass, בלי אלמנטים שמרגישים "AI" גנרי. header נגלל עם העמוד ולא צף |
| עמוד עסק | סגנון פרופיל פייסבוק. 3 רמות: ברירת מחדל + אישי **בחינם**, PRO בתשלום — מוצג כ"בקרוב" עם רשימת המתנה עד הסליקה |
| PRO (מתוכנן) | ראשונים בחיפוש, תג PRO וטבעת זוהרת, ערכות פרימיום, רקע מתחלף וגלריה בלי הגבלה, באנר מבצעים, קביעת תור, סטטיסטיקות, כתובת אישית |
| מרחק | ממרכז העיר של העסק (טבלת `cities`). אפשר לשדרג לכתובת מדויקת (`location_source = address`) בלי לשנות את החיפוש |
| צבעי מותג | לפי הלוגו: ירוק → תכלת → כחול (`bg-kami`, `text-kami`), כפתורים ראשיים בגרדיאנט תכלת-כחול |
| עמודים לא מנוהלים | רק מידע ציבורי בסיסי, בלי ביקורות ובלי תוכן שיווקי בשם העסק. מסומנים "עסק שלא אומת" עם הבהרה שהמידע נאסף ממקורות פומביים ולא נבדק מול העסק. כל בקשת בעלות נבדקת ידנית; עד האישור למבקש אין שום הרשאה. הסרה אפשרית גם בלי חשבון (טופס צור קשר), בלי תנאי או תשלום |
| ביקורות | עולות מיד, בלי אישור מראש. הגנה על עסקים: דיווח + הסתרה אוטומטית אחרי 3 דיווחים עד החלטת הצוות. לא מסירים ביקורת רק כי היא שלילית |
| דף הבית | בלי כותרת "קטגוריות": הקטגוריות כשורת אייקונים מתחת לחיפוש |
| ימי אימוץ | כרגע רק פוסטרים של עמותות (הבעלים מעלה). גלריה של חיות בודדות — אולי בהמשך |
| מודעות | עד 3 מתחלפות למיקום ליום; פופאפ אחד ליום (יקר יותר, כולם רואים). ימי אימוץ לעמותות בחינם, תמורת פרסום Kami אצלן ברשתות |
| תנאים | גרסה ב-`TERMS_VERSION` (`src/lib/legal.ts`), נשמרת בכל פרופיל חדש. שינוי מהותי → להעלות גרסה ולהודיע 14 יום מראש |
| סינון תוכן | רשימת מילים גסות בטבלה `banned_terms`, נאכפת בטריגר על עסקים ותמונות |

## החלטות פתוחות
1. **תקופת חינם ל-PRO לעסקים הראשונים?** (לפני שלב 6) המלצה: 3 חודשים חינם.

## סביבות
- אתר חי: https://petlink-five.vercel.app (Vercel, ענף `main`)
- מסד נתונים: Supabase, פרויקט `ebpazvemknplklqhiagf`, פרנקפורט
- כל migration חדש רץ ידנית ב-Supabase → SQL Editor, לפני מיזוג ה-Pull Request.
